# Variables de entorno para VIN Tracker

# Configuración de Base de Datos PostgreSQL (Podman)
DB_HOST=localhost
DB_PORT=5432
DB_DATABASE=vin_tracker
DB_USER=vintracker
DB_PASSWORD=CHANGE_ME

# Puerto del servidor backend
PORT=3001

# Configuración adicional (opcional)
NODE_ENV=development

# OCR.space API — para importación por imagen (OCR)
# 1. Regístrate gratis (solo email) en: ocr.space/OCRAPI
# 2. Recibes la API key por email inmediatamente
# Límite gratis: 25.000 requests/mes, sin tarjeta ni billing
OCR_SPACE_API_KEY=your_ocr_space_api_key_here
